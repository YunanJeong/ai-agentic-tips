# 하네스 / 하네스 엔지니어링

## 하네스란

**모델은 텍스트 함수다.** 문자열 넣으면 문자열 나온다. 그게 전부다. 파일 못 읽고, 명령 못 돌리고, 기억 못 한다.

**하네스는 그 함수를 감싸서 "에이전트처럼 보이게" 만드는 실행 계층이다.**

"파일 읽어보겠습니다"라고 할 때 모델이 실제로 하는 일은 이런 텍스트를 뱉는 것뿐이다:

```
{name: "Read", input: {file_path: "/tmp/foo.py"}}
```

이건 그냥 글자다. 모델은 파일을 열 능력이 없다. 하네스가 이걸 파싱해서 진짜 `fs.readFile`을 호출하고, 결과를 다음 프롬프트에 텍스트로 붙여서 모델을 다시 부른다.

하네스가 없으면 모델은 허공에 "Read /tmp/foo.py"라고 중얼거리는 것으로 끝난다.

> **모델이 결정하고, 하네스가 실행한다.**

### 하네스가 실제로 하는 것 (Claude Code 기준)

- **루프** — API 호출 → tool_use 발견 → 실행 → 결과 되돌림 → 반복
- **툴 구현체** — `Read`는 실제 파일시스템, `Bash`는 실제 child_process, `Edit`는 실제 문자열 치환 + 쓰기
- **컨텍스트 조립** — 매 턴 프롬프트에 뭘 넣을지: 시스템 프롬프트, CLAUDE.md, 툴 정의, 히스토리
- **컨텍스트 관리** — 길어지면 요약(compaction)
- **권한 게이트** — 이 명령 실행 전에 물어볼지 말지
- **렌더링** — 터미널 UI, 마크다운, diff 표시
- **상태** — 세션 파일, 트랜스크립트, 메모리 디렉터리
- **설정** — settings.json, hooks, keybindings

### 왜 이 단어를 쓰나

같은 모델 가중치가 하네스에 따라 완전히 다르게 동작한다.

- **Claude Code** — 파일시스템 + bash + 툴 루프
- **claude.ai** — 웹 검색, 아티팩트, 파일 업로드
- **API 직접 호출** — 내가 만든 툴만

같은 모델인데 할 수 있는 일이 전혀 다르다. 하네스가 실제 작업을 한다는 증거다.

어원은 eval 쪽이다. 벤치마크 측정용 스캐폴딩을 harness라고 불렀고 그 말이 넘어왔다. 마구(馬具)라는 뜻이니 비유도 정확하다 — 말은 힘을 내고, 마구가 그 힘을 수레에 연결한다.

---

## 하네스 엔지니어링이란

> 하네스를 처음부터 만드는 것도 포함되지만, 실무에서 이 말이 가리키는 건 대개 **기존 하네스의 동작 구조를 프롬프트 외적으로 설계·구성하는 것**이다.

통째로 만드는 건 이제 잘 안 한다. Agent SDK, Tool Runner, Managed Agents가 하네스를 대주니까. 남은 실제 작업이 "기존 하네스에 부품 달기"라서 그게 무게중심이 됐다. 통째로 만드는 걸 가리킬 때는 보통 그냥 "에이전트 만든다"고 한다.

### 왜 이 말이 생겼나 — 대비항은 프롬프트 엔지니어링

모델이 약할 때는 병목이 문장이었다. "CRITICAL: YOU MUST..." 같은 걸로 억지로 끌어냈다. 모델이 좋아지면서 병목이 옮겨갔다 — **툴을 뭐로 쪼갤지, 컨텍스트에 뭘 남길지, 실패를 어떻게 되먹임할지.** 문장이 아니라 구조 문제다.

### 판별 기준

> **모델이 보는 텍스트를 고치면 프롬프트 엔지니어링, 모델 주변 기계를 고치면 하네스 엔지니어링.**
>
> 파라미터만 돌리면 둘 다 아니다 (그건 운영).

| 층 | 하는 일 | 하네스 엔지니어링? |
|---|---|---|
| **만들기** | 루프 설계, 툴 표면 설계, 컨텍스트 전략, 승인 게이트 | ✅ 원래 의미 |
| **확장하기** | hooks, settings.json, 서브에이전트, MCP, 툴 권한 | ✅ 실질적으로 포함 |
| **호출 조절** | effort, max_tokens, model 바꾸기 | ❌ 운영 |
| **텍스트 고치기** | CLAUDE.md, 스킬, 시스템 프롬프트 | ❌ 프롬프트 엔지니어링 |

---

## 예시 — Claude Code에서

### 하네스 엔지니어링인 것

**PreToolUse hook으로 파일 차단**
`settings.json`에 hook을 걸어 경로 조건에 맞으면 툴 호출을 실행 전에 죽인다. 모델 판단이 개입할 여지가 없다.

```json
{ "hooks": { "PreToolUse": [{ "matcher": "Read|Grep|Bash",
  "hooks": [{ "type": "command", "command": "~/.claude/block-tfvars.sh" }] }] } }
```

**서브에이전트 정의**
`.claude/agents/*.md`에서 그 에이전트에 **어떤 툴만 줄지** 정한다. Edit/Write를 안 주면 파일을 못 고친다. 컨텍스트도 격리돼서 메인 대화가 오염되지 않는다.

**권한 허용목록**
`settings.json`의 permissions로 승인 게이트 위치를 옮긴다.

**MCP 서버 부착**
프롬프트 한 글자 안 바꾸고 툴 목록에 항목이 늘어난다.

### 하네스 엔지니어링이 아닌 것

**메모리 / 스킬 / CLAUDE.md** → 프롬프트 계층. 모델이 그렇게 하도록 **지시**하는 것이고, 모델이 실수하면 뚫린다.

**`/model`, `/config`의 effort, `/fast`** → 호출 파라미터. 기계는 그대로고 다이얼만 돌린 것.

---

## 예시 — 직접 만드는 앱에서

### 하네스 엔지니어링인 것

**툴 표면 설계 — bash를 쪼갤지**
`bash -c "curl -X POST ..."`는 하네스가 불투명한 문자열만 받아서 게이트를 걸 수 없다. `send_email(to, subject, body)`로 승격하면 인자를 보고 확인 모달·감사 로그·도메인 제한이 가능하다. **되돌리기 어려운 동작을 전용 툴로 뽑는 판단**이 핵심.

같은 이유로 `grep`/`glob`은 병렬 안전 표시가 가능하지만, bash로 넘어오면 안전한 grep과 위험한 `git push`를 구분할 수 없어 전부 직렬화해야 한다.

**Tool Runner 턴별 훅**
루프는 SDK가 돌리지만 매 이터레이션이 **툴 실행 전에** 넘어온다. pending 호출을 보고 승인/거부하거나, 결과를 가로채 재시도하거나, `cache_control`을 붙일 수 있다.

**컨텍스트 전략**
context editing(오래된 툴 결과 **삭제**) / compaction(**요약**) / memory(파일로 외부화) 중 뭘 쓸지. "툴 출력이 커서"인지 "대화가 길어서"인지에 따라 답이 다르다.

**vault로 시크릿 격리**
샌드박스에는 플레이스홀더만 넣고 진짜 값은 egress에서 치환한다. 컨테이너 안 코드는 — 모델이 방금 쓴 코드 포함 — 키를 읽을 수가 없다.

**Programmatic tool calling**
모델이 스크립트를 쓰게 하면 툴 결과가 코드로 돌아가고 **모델 컨텍스트에 안 들어온다.** 최종 출력만 올라온다. 토큰이 중간 결과 크기가 아니라 최종 출력 크기에 비례한다.

### 하네스 엔지니어링이 아닌 것

**"API 키를 노출하지 마"를 시스템 프롬프트에 쓰기** → 프롬프트 계층. vault 격리와 목표는 같고 계층이 다르다.

**effort / max_tokens 튜닝, 모델 교체** → 호출 파라미터.

---

## 관통하는 패턴

| 프롬프트 계층 (아님) | 하네스 계층 (맞음) |
|---|---|
| "tfvars 읽지 마" | PreToolUse hook이 거부 |
| "이 에이전트는 읽기만 해" | tools 목록에서 Edit 제거 |
| "키를 노출하지 마" | vault가 egress에서 치환 |
| "매번 포맷해" | PostToolUse hook이 실행 |

**부탁 → 불가능.** 이 전환이 하네스 엔지니어링이다.

마지막 행이 특히 중요하다 — **"앞으로 매번 ~해라"는 모델에게 부탁해서 보장되지 않는다.** 모델은 매 턴 새로 시작하니까. 그건 반드시 hook이어야 한다.

---

## 부록 — 경계 판별 치트시트

문제가 생겼을 때 어느 쪽인지 알면 어디를 고칠지 정해진다.

| 증상 | 어느 쪽 | 고치는 곳 |
|---|---|---|
| 파일 내용을 지어냄 | 모델 | 프롬프트 |
| 읽겠다고 말만 하고 툴 호출 안 함 | 모델 | 프롬프트 |
| 말이 장황함, 범위 넘어감 | 모델 | CLAUDE.md |
| 권한 프롬프트가 안 뜸 | 하네스 | settings.json |
| 매번 같은 명령에 승인 요구 | 하네스 | permissions 허용목록 |
| 컨텍스트가 엉뚱하게 잘림 | 하네스 | /config, 세션 관리 |
| "X 할 때마다 자동으로 Y"가 안 됨 | 하네스 | hooks |

## 부록 — 하네스를 어디까지 직접 쓰나

**하네스**와 **배포**를 누가 대는지로 갈린다.

| 방식 | 내가 쓰는 코드 | 하네스 / 배포 |
|---|---|---|
| 직접 루프 | `while stop_reason == "tool_use"` 전부 | 둘 다 나 |
| **Tool Runner** (`client.beta.messages.tool_runner`) | 툴 함수만 | 루프는 SDK, 호스팅은 나 |
| **Managed Agents** | 에이전트 설정 + 툴 결과 | 둘 다 Anthropic (세션별 샌드박스까지) |
| **Claude Agent SDK** | 프롬프트 + 옵션 | Claude Code 하네스 통째로, 호스팅은 나 |

- **Tool Runner** — 대부분 정답. 루프를 손으로 안 쓰면서도 턴별 훅으로 승인 게이트·에러 개입·결과 수정·재시도가 다 된다
- **Managed Agents** — 루프도 컨테이너도 Anthropic이 돌린다. 스케줄 실행, 장시간 세션, 버전 관리되는 설정이 필요하면 이쪽
- **Claude Agent SDK** — Claude Code를 라이브러리로 쓰는 것. Read/Write/Edit/Bash/Grep 내장 툴 + 서브에이전트 + hooks가 딸려온다

> **Tool Runner ≠ Claude Agent SDK.** 이름이 비슷해서 자주 섞는다. 전자는 일반 SDK(`anthropic`)의 헬퍼로 **내가 정의한 툴만** 돌린다. 후자는 별도 패키지(`claude-agent-sdk`)로 **내장 툴이 다 들어있는** Claude Code 하네스다.

첫 질문은 항상 "에이전트가 정말 필요한가"다. 단발 호출이나 코드로 제어하는 워크플로로 되는 일에 에이전트 루프를 씌우면 비용·지연·디버깅 난이도만 올라간다.
